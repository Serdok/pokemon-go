// Package models
/*
	The package models defines all structs used in the application and in the database.
	Each struct will have their fields but also the tags to correctly convert to/from JSON,
	but also to/from custom marshals
*/
package models
